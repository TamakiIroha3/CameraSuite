<Window x:Class="CameraSuite.ViewerHost.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vlc="clr-namespace:LibVLCSharp.WPF;assembly=LibVLCSharp.WPF"
        xmlns:vm="clr-namespace:CameraSuite.ViewerHost"
        mc:Ignorable="d"
        Title="CameraSuite Viewer"
        Width="1200"
        Height="800"
        Background="#111">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </Window.Resources>
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0 0 0 12">
            <TextBlock Text="Camera Suite Viewer" FontSize="20" FontWeight="Bold" />
            <TextBlock Text="{Binding Streams.Count, StringFormat=' - Active Streams: {0}'}" Margin="16 0 0 0" />
        </StackPanel>

        <ScrollViewer Grid.Row="1">
            <ItemsControl ItemsSource="{Binding Streams}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="{Binding GridColumns}" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:StreamViewModel}">
                        <Border Margin="6"
                                BorderBrush="#333"
                                BorderThickness="1"
                                CornerRadius="6"
                                Background="#222"
                                Padding="8">
                            <StackPanel>
                                <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" />
                                <TextBlock Text="{Binding Status}" Margin="0 2 0 8" FontSize="12" Foreground="#AAA" />
                                <vlc:VideoView MediaPlayer="{Binding MediaPlayer}" Height="240" />
                                <TextBlock Text="{Binding LastError}" Foreground="#DD6666" FontSize="12"
                                           TextWrapping="Wrap" Visibility="{Binding LastError, Converter={StaticResource NullToVisibilityConverter}}" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>
